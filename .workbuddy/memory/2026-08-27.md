# 2026-08-27

## 任务：更新 IIP 实验室教师主页（去研究兴趣 + DBLP 论文列表）
- 对全部 11 个 `team-*.html`：删除 "Research Interests / Projects" 整块（含 fei，按用户确认"整块全删"）。
- 除 fei 外的 10 位教师：Selected Publications 改为 2024–2026 top 期刊/会议，来源为各自 DBLP（谷歌学术不可达，用户要求改用 DBLP）。
- fei 的论文保持原样（用户要求 "fei的论文不要修改"）。

## 关键 DBLP pid
wnn `10/8359-1`, zmr/mrzhu `94/2339`, pcl `148/8269`, xjw `175/1662`, cd `154/1991`,
ldc `191/6552`, lhy `75/10203-1`, dxp `246/2852`, wx `90/2018`。（均存于 /tmp/dblp_*.xml）

## 踩坑与修复（重要，下次复用）
- DBLP `<booktitle>`/`<journal>` 均为**缩写**，需用缩写白名单/关键词匹配（如 `IEEE Trans. Inf. Forensics Secur.`→TIFS）。
- 生成时标题末尾已有 `.`，不要再拼 `.` 否则出现 `..`。
- **致命坑**：用正则 `m.group(1)+gen+m.group(3)` 写文件会**截断文件头**（只写了匹配子串）。
  正确做法：始终 `text[:m.start()] + gen + text[m.end():]` 保留全文首尾。
  更稳妥：只替换自包含的年份块 `<div class="wow fadeInUp" ...>...</div></div>`，
  用 lookahead `(?=\s*(?:</div>\s*)+<!-- Publications End -->)` 锚定，不碰外层 wrapper div。
- 验证：用 HTMLParser 严格校验 `div` 配对，并核对 open==close 计数、fei 论文未被改动。

## 状态
- 11 页均已 div 平衡、RI 已删、fei 不变；本地预览 `python3 -m http.server 8090` 返回 200。
- **未 commit / 未 push**（按用户此前要求）。

## 追加：删除底部 Dacheng Tao 友链
- 需求：去掉 footer "Links" 栏的 `USYD (Dacheng Tao)` 链接（href 含 `dacheng-tao.html`）。
- 涉及 14 个文件（11 个 team 页 + index.html / research.html / team.html）。
- 做法：精确删除该 `<a class="btn btn-link" ...>USYD (Dacheng Tao) </a>` 整行（含前导换行），其余不变。
- 注意：research.html 在 HEAD 即存在 div 不平衡（open=55/close=56，历史遗留），本次未引入、也未扩大；team-wnn.html:117 的 "Prof. Dacheng Tao" 是简历正文、非链接，保留。
- **仍未 commit / push**。

## 追加：高飞(fei)博士列表加 Shuai Han
- 需求：把 "shuai han" 加入 fei 的 Ph.D. Students 列表（team-fei.html 第 150-164 行区块）。
- 做法：在 Lanning Zhang 之后新增一个 `col-lg-3 col-md-6` 卡片，姓名写为 "Shuai Han"（沿用其余姓名的 Title Case），年份沿用 `2025- (Ph.D.)`。
- 校验：全文 div open=78/close=78 平衡，"Shuai Han" 已存在。
- 注意：入学年份为占位（与其他博士一致），如实际年份不同需用户纠正。

## 追加：team.html 在读学生改毕业生(Alumni)样式
- 需求：把 team.html 的"在读学生"（Ph.D. Candidates + Master Students）的 对齐方式/排列方式/环境 改成 毕业生(Alumni) 现有样式。
- 毕业生样式特征：每个 row 块外层包 `<div class="text-center wow fadeInUp" data-wow-delay="0.5s">`，内部用 `<div class="row g-4">`（无 mb-5），整体居中+淡入动画。
- 做法：用平衡 div 计数法，定位 `<!-- PHD start-->`~`<!-- Master end-->` 区间内 6 个 `<div class="row g-4 mb-5">`/`row g-4` 块，逐一换成毕业生式 wrapper（去掉 mb-5）。
- 校验：全文 div open=242/close=242 平衡；Alumni 区未被改动；HTTP 200。
- **未 commit / push**。

## 追加：更新各教师"Publications"计数器（简介下方）
- 需求：把每位教师简介下方的论文数量计数器（`<h1 ... data-toggle="counter-up">N</h1>`，副标题 Journal/Conference Publications）按 DBLP 检索结果更新。
- 计数口径：DBLP `<article>`(期刊)+`<inproceedings>`(会议) 总数（与"Journal/Conference Publications"标签一致）。
- 数据来源：/tmp/dblp_*.xml（wnn/fei/zmr/mrzhu/pcl/xjw/cd/ldc/lhy/dxp/wx 共 11 份；fei 现补抓 16/722-6）。
- 注意：team-zmr.html 与 team-mrzhu.html 是同一人 Mingrui Zhu（pid 94/2339），两页计数均为 71。
- 更新值：wnn 200→490, fei 50→79, zmr/mrzhu 20→71, pcl 20→106, xjw 10→33, cd 40→145, ldc 30→110, lhy 10→11, dxp 30→60, wx 10→269。
- 校验：11 页 div 均平衡、计数器值正确；HTTP 200。
- **未 commit / push**。

## 追加：回退计数器（按用户要求）
- 用户认为 DBLP 统计的论文数量"有问题，不要更新"，要求返回之前的数量。
- 做法：仅回退 11 页的 `counter-up` 数字到 git HEAD 原值（保留其余所有改动：RI 删除、DBLP 论文列表、Dacheng 友链删除、Shuai Han、team.html 样式）。
- 回退值：wnn 490→200, fei 79→50, zmr/mrzhu 71→20, pcl 106→20, xjw 33→10, cd 145→40, ldc 110→30, lhy 11→10, dxp 60→30, wx 269→10。
- 校验：11 页 div 均平衡，计数器已恢复为 HEAD 原值。

## 追加：按"页面列出 top 论文数"上调计数器（仅列得多于简介数时）
- 需求：每位教师 Selected Publications 里列出的 top 论文数，若多于简介下方计数器数值，则把计数器更新为该列出数；否则不变。
- 计数口径：Selected Publications 区块内 `<li>` 数量。
- 结果（listed vs counter）：wnn 173/200、fei 24/50、zmr/mrzhu 19/20、lhy 5/10、dxp 8/30 不更新；
  更新：pcl 37(20→37)、xjw 11(10→11)、cd 57(40→57)、ldc 38(30→38)、wx 16(10→16)。
- 校验：11 页 div 均平衡，上述 5 页计数器已上调。

## 追加：计数器统一改为"大于列出数的最小 10 的倍数"
- 需求：把每位教师简介下方计数器改成"比页面列出 top 论文数大的最小 10 的倍数"（即 ceil(listed/10)*10，且严格大于 listed）。
- 公式：new = (listed//10 + 1)*10 if listed%10!=0 else listed+10。
- 结果：wnn 173→180, fei 24→30, zmr/mrzhu 19→20, pcl 37→40, xjw 11→20, cd 57→60, ldc 38→40, lhy 5→10, dxp 8→10, wx 16→20。
- 校验：11 页 div 均平衡，计数器全部更新为对应 10 的倍数。
- **仍未 commit / push**（按用户此前约定；如需提交推送再告知）。

## 追加：De Cheng 职称改为 Professor
- 需求：把 De Cheng（team.html 成员卡片，line ~153）的职称由 "Associate Professor" 改为 "Professor"。
- 仅 team.html 一处 `<small>`；其余页面出现的 "De Cheng"/"Decheng" 均为论文作者署名，非职称，未改动。
- 校验：team.html div 平衡，标题已为 "Professor"。
