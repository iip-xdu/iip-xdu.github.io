# 2026-09-23

## 语言切换图标去掉文字标签
- 用户要求右上角切换语言图标旁不再显示「中文/EN」文字。
- 原因：js/i18n.js 的 `init()` 会自动给 `.lang-toggle` 追加 `<span class="lang-label">`（显示 中文/EN），`applyMeta()` 还会更新其文字。
- 修改：删除 i18n.js 中创建 lang-label 的代码块及 applyMeta 内的 label 更新逻辑。各 HTML 页面本身无硬编码标签，故全站生效。
