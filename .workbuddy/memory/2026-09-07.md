# 2026-09-07

## 任务：team.html 学生名单取消按入学年份强制换行
- 需求：成员名单里的学生名单（Ph.D. Candidates / Master Students）不按入学年份（2024/2025/2026）强制换行。
- 原状：每个 year 各是一个独立的 `<div class="row g-4">`（包在各自 `text-center wow fadeInUp` 里），等于按年份强制分块换行。
- 改法：把 `<!-- PHD start-->~<!-- PHD end-->` 与 `<!-- Master start-->~<!-- Master end-->` 两个区内、所有 `col-lg-3 col-md-6` 学生卡片合并进**单个** `<div class="row g-4">`（保留一个 `text-center wow fadeInUp` wrapper，保留 Students/Ph.D. Candidates/Master Students 标题）。
- 卡片数：Ph.D. 12（5+4+3），Master 61（17+17+27）。Alumni 区未动（仍 1 行 73 卡）。
- 踩坑：第一版用 `region.index('data-wow-delay="0.5s"')` 切分，把行块开标签 `<div ... data-wow-delay="0.5s">` 从中间切开，遗留悬挂的 `<div class="text-center wow fadeInUp"`。已用"按 h1 标题闭合 `</div>` 定位 header"的方式重建修复，broken_open_tags=0。
- 校验：team.html div 平衡；Ph.D./Master 各 1 个 row 块、0 破损标签；Alumni 不变。
- **未 commit / 未 push**。

## 任务：Master Students 增加 2025 级 吴欣雨
- 在 `team.html` 的 `<!-- Master start-->~<!-- Master end-->` 连续名单中，新增 1 张卡片：`<div class="col-lg-3 col-md-6"><h5 class="mb-0">Xinyu Wu</h5><small>2025-</small></div>`。
- 命名按现有名单的拼音风格（吴欣雨→Xinyu Wu，姓置后），未加 `data-page-node-id`（与之前手动加卡一致）。
- 位置：排在 2025 级卡片组末尾（Xuanrong Feng 之后）。
- 校验：Master 区卡片 61→62；team.html div 平衡；Xinyu Wu 存在。
- 备注：若用户想用中文「吴欣雨」原样、或把该生归到某位导师的个人主页（team-*.html 的 Postgraduates），需再调整。

## 任务：Ph.D. Candidates 增加 2027 级 吴志宏
- 在 `team.html` 的 `<!-- PHD start-->~<!-- PHD end-->` 连续名单中，新增卡片：`<div class="col-lg-3 col-md-6"><h5 class="mb-0">Zhihong Wu</h5><small>2027-</small></div>`。
- 命名沿用拼音风格（吴志宏→Zhihong Wu，姓置后），排在最后一张 2026 级（Na Zhang）之后。
- 校验：Ph.D. 区卡片 12→13；team.html div 平衡；Zhihong Wu 存在。
- **未 commit / 未 push**。

## 任务：index.html 首页 "Recent News" 重写（仅 2026 年）
- 旧内容：Xinpeng Ding 视角的英文论文列表（仅论文录用）。
- 新内容：用户提供的中文实验室新闻（2026 全年 12 条），改写为更规范的书面语，覆盖论文录用 + 优秀学位论文奖 + 国奖 + 三秦英才 + 毕业去向 + 签约就业 + TPAMI/IJCV 等期刊录用，不止论文录用。
- 实现：用 Python 正则定位 `<h4>...Recent News...</h4>` 后的 `<ul>...</ul>`，整体替换为新 `<ul>`（每条 `<li>` 以 `<span class="text-primary fw-bold">2026年X月</span>` 起首；多论文条目内层嵌套 `<ul>`）。年份标签维持 "2026"。
- 校验：div 计数与 HEAD 一致（65/65），未引入新 div；index.html 整体在 HEAD 即存在 1 个历史遗留 stray `</div>`（与本次无关）。12 条 2026 新闻均在。
- **未 commit / 未 push**。

## 首页新闻改英文版
- index.html 的 Recent News（ul data-page-node-id="UirpfSxB1QdfYLA5QqMC9K"）改为纯英文、仅 2026 年，共 14 条；ECCV 只写录用数量与方向，不再列出论文标题。
- 每条控制在一到两行，日期格式改为 "Jun. 2026" 等。
- css/style.css 末尾新增 `.news-name`（橙色底 + 深棕字 + 600 字重）用于高亮人名；人名统一拼音（姓在后，如 Nuoyan Zhou）。
- 校验：div 平衡与 HEAD 一致（-1，历史遗留），新闻区无残留中文。
- research.html 三个 Research Projects 卡片：外层加 `project-card` + `flex-column flex-lg-row align-items-lg-center`，窄屏（<992px）图在上、文字在下；css/style.css 加 @media 规则（图片 100% 宽、max-height 220px、压缩上下 padding）。
- team.html Alumni 区按用户权威名单重建：博士 18 人（2016-2026，升序）、硕士 49 人（2012-2026，升序），姓名拼音 + "年份, 单位"，单位用缩写（XDU/HKUST/HKBU/CQUPT/CETC Institute 54 等）。
  - 修正的错误：年份与去向错配（如 Yihan Xu、Dongxin Chen、Chaohua Shi、Si Shi、Zhixin Wu、Keyi Chen、Jianhang Chen、Yibo Xu 等整体后移一年）；重复条目（Liting Sun、Yifan Jiang、Shiyin Dong、Zikai Wei）；不存在的条目（Siji Yi、Zhi Xu、Wenkai Chen、Zhipeng Wu、Shuai Han）。
  - 依用户要求删除不在名单中的旧条目：Bing Xiao(2010)、Nannan Wang(2015)、Yuqian Zhang(2019, Xiaomi)。
  - 保留原有个人主页链接（张声传/张铭津/Adeel Akram/曹兵/马卓奇/郝毅/周大为）。
  - 修复 team.html 结尾遗留的 2 个多余 </div>（此前 div 平衡为 -2，现已恢复 0）。
